class Operations {

    add(result: number, number: number): number {
        return 0;
    }

    subtraction(result: number, number: number): number {
        return 0;
    }

    division(result: number, number: number): number {
        return 0;
    }

    multiplication(result: number, number: number): number {
        return 0;
    }
}

export default Operations;